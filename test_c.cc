#include <iostream>

using namespace std;

int main() {

    cout << "Hello World" << endl;
/*
    //variables
    int x = 0;

    //command line inputs
    for(int i = 0; i < 5; i++){
        x += i;
    }

    cout << x << endl;

    //set random seed if seed hasn't been set
    if(!setSeed){
        srand(time(NULL));
    } else {
        srand(seed);
    }

    return 1;
*/
}

